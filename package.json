{
  "name": "full-stack",
  "version": "1.0.0",
  "scripts": {
    "build": "if-env TYPE=server && (cd front-end && npm install) || (cd back-end && npm install)",
    "start": "npm run server & npm run client",
    "server": "node ./backend/server.js",
    "client": "cd front-end && npm start"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "if-env": "^1.0.4"
  }
}
