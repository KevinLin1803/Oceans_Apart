function addTask() {

    document.getElementById("task-1").innerText = "hello"
    
    // Not sure if this first line works either

    // let task_name = document.getElementsById("task-input").value
    // let task_list = document.getElementsById("tasks")

    // let newtask_item = document.createElement("li")
    // new_task_item.textContent = `${task_name}`

    // task_list.appendChild(newtask_item)
}

export default addTask
